package SeleniumSession;

//import org.openqa.selenium.WebDriver;
//import org.openqa.selenium.chrome.ChromeDriver;
//import org.openqa.selenium.SearchContext;
//import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOption {

	public static void main(String[] args) {

		// 1.browser specific:
		// ChromeDriver drive = new ChromeDriver();
		// FirefoxDriver driver1 = new FirefoxDriver();

		// 2.WD = new CD() - valid top casting - recommended - Local Execution
		// WebDriver driver = new ChromeDriver();

		// 3.SD = new CD()- valid top casting -not recommended
		//SearchContext driver = new ChromeDriver();
		
		//4.RWD = new CD()- valid top casting - recommended - Local Execution
		//RemoteWebDriver driver = new ChromeDriver();
		
		//5.WD = new RWD() - valid top casting - recommended -GRID- used for remote execution
		 //WebDriver driver = new RemoteWebDriver(remoteAdress, Capabilities);
		
		//6.SD  = new RWD() - valid top casting -not recommended
		//SearchContext driver = new RemoteWebDriver(remoteAdress, Capabilities);

	}

}
